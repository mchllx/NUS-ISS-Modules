export interface Friend {
    name: string
    email: string
    phone: string
    dob: string
    friend: boolean
}

export interface Contact {
    name: string
    email: string
    phone: string
    dob: string
    friend: boolean
}